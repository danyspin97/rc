foreground { fdmove -c 1 2 s6-echo "Starting mount-tmp" }

foreground { mount /tmp }
foreground
{
	s6-mkdir -p /tmp/paludis/build
	s6-chmod 443 /tmp/paludis/build
	s6-chmod g+rwx /tmp/paludis/build 
}
